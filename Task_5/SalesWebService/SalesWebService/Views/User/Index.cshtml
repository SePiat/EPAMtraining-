@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityUser>
@{
    ViewBag.Title = "List of users ";
}

<h2 class="text-info">List of users</h2>
<table class="table ">   
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            
            <td>
                <form asp-action="Delete" asp-route-Id="@user.Id" method="post">
                    <a class="btn btn-light" asp-action="Edit" asp-route-id="@user.Id">ChangeEmail</a>
                    <a class="btn btn-light" asp-action="ChangePassword" asp-route-id="@user.Id">Change password</a>
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </td>
        </tr>
    }
</table>
<a class="btn badge-light"  asp-action="Create">Add user</a>
